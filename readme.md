# Drizzle ORM test project

## Description

```bash
# install dependencies
yarn

# start mysql
docker-compose up -d

# generate sql file
yarn db:generate

# run migration
yarn db:deploy

# run app code
yarn dev
```
